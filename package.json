{
  "name": "webgrapgh",
  "private": true,
  "description": "Repo-root tooling: lint/format across shared/ and all frontends.",
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4"
  }
}
